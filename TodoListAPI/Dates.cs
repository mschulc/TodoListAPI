namespace TodoListAPI
{
    //Enum that helps cast the data serching
    public enum Dates
    {
        today = 0,
        nextday = 1,
        currentweek = 2,
        currentmonth = 3,
        currentyear = 4
    }
}
